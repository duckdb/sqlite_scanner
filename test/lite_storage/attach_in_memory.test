statement ok
LOAD '../build/debug/extension/sqlite_scanner/sqlite_scanner.duckdb_extension';

statement ok
ATTACH ':memory:' AS mem (TYPE SQLITE)

statement ok
CREATE TABLE mem.integers(i INTEGER)

# FIXME: need to re-use same in-memory database
statement ok
INSERT INTO mem.integers VALUES (42)

query I
SELECT * FROM mem.integers
----
42
